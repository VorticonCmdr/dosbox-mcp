__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/
.sessions/
.claude/
# Library, not an app: consumers resolve their own versions from the
# ranges in pyproject.toml, so this lockfile is a local dev artifact.
uv.lock
